# My Gallery

Native Kotlin Android gallery application.

## Build on GitHub
Push this folder to a GitHub repository. Open **Actions → Build My Gallery APK → Run workflow**.
After completion, download the **MyGallery-APK** artifact.

The workflow installs Gradle 8.11.1 directly, so a Gradle wrapper JAR is not required for GitHub Actions.
